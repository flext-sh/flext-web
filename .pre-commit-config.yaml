# Generated by flext_infra codegen for flext-web — DO NOT EDIT
repos:
  - repo: local
    hooks:
      # Why: each make verb is its own pre-commit/pre-push hook so the operator
      # sees per-step status (Passed/Failed) natively via the pre-commit runner.
      # CI=Y is never injected here, because it skips the
      # lint/format/pyrefly gates that must block a push (9b604d43).
      # make test never inherits that token either (mro-v4p5 forbids pytest
      # under CI=Y).
      # A step declaring gates_skip (mro-ucxgl) narrows CHECK_GATES for the hook
      # context only: local `make check` and the `docs` verb keep the full set.

      - id: flext-pre-commit-setup
        name: make setup
        language: system
        entry: >-
          make setup
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-gen
        name: make gen APPLY=Y
        language: system
        entry: >-
          make gen APPLY=Y
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-fmt
        name: make fmt APPLY=Y
        language: system
        entry: >-
          make fmt APPLY=Y
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-fix
        name: make fix APPLY=Y
        language: system
        entry: >-
          make fix APPLY=Y
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-check
        name: make check
        language: system
        entry: >-
          CI=Y CHECK_GATES=lint,pyrefly,mypy,pyright,security,smells make check
        pass_filenames: false
        always_run: true
        stages: [pre-commit]


      - id: flext-pre-push-setup
        name: make setup
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset $(git rev-parse --local-env-vars); make setup'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-gen
        name: make gen APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset $(git rev-parse --local-env-vars); make gen APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-fmt
        name: make fmt APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset $(git rev-parse --local-env-vars); make fmt APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-fix
        name: make fix APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset $(git rev-parse --local-env-vars); make fix APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-check
        name: make check
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset $(git rev-parse --local-env-vars); CHECK_GATES=lint,pyrefly,mypy,pyright,security,smells make check'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-test-cache-clear
        name: make test WHAT=cache-clear APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset $(git rev-parse --local-env-vars); make test WHAT=cache-clear APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-test-full
        name: make test WHAT=full
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset $(git rev-parse --local-env-vars); make test WHAT=full'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

