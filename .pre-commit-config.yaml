# Generated by flext_infra codegen for flext-web — DO NOT EDIT
repos:
  - repo: local
    hooks:
      - id: flext-canonical-workflow
        name: FLEXT canonical workflow
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'make gen APPLY=Y && make fmt APPLY=Y && make fix APPLY=Y && make check && make test'
        pass_filenames: false
        always_run: true
