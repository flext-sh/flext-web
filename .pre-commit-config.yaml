# Generated by flext_infra codegen for flext-web — DO NOT EDIT
repos:
  - repo: local
    hooks:
      # Why: fast fixes run before each commit; expensive gates run before push.
      - id: flext-pre-commit
        name: FLEXT pre-commit workflow
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'make fmt APPLY=Y && make fix APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
      - id: flext-pre-push
        name: FLEXT pre-push workflow
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'make gen APPLY=Y && make check && make test'
        pass_filenames: false
        always_run: true
        stages: [pre-push]
