"""Django settings package for FLX Web application.

This package contains environment-specific Django settings modules.
The appropriate settings module is selected based on the DJANGO_SETTINGS_MODULE
environment variable or defaults to development settings.

Available settings modules:
    - base: Common settings shared across all environments
    - development: Development environment with debug tools
    - production: Production environment with security hardening
"""
