# @flext-generated: continuous
# @flext-owner: flext-infra templates + config/beads.yaml identity
# @flext-adjust: edit config/beads.yaml identity; never this file
# @flext-regenerate: make gen
# Generated by `flext-infra gen conform`.
# Source: repository-local Beads identity only. Server, mode, endpoint and every
# other runtime setting come from the environment, never from this file.
# bd reads `issue_prefix` (underscore). Do not emit `issue-prefix`.
issue_prefix: "flext"
