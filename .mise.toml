# @flext-generated: continuous
# @flext-owner: flext-infra/config/codegen.yaml + flext-infra/src/flext_infra/templates/project/base/.mise.toml.j2
# @flext-adjust: edit the owner configuration or template; never this projection
# @flext-regenerate: make gen
# === SECTION: header (managed) ===
# Source: template (base/.mise.toml.j2)
# Free: no
# End SECTION: header
# NOTE: mise carries the Python runtime and native tools from the config SSOT.
# Python dependency declarations remain owned by pyproject.toml. Tools are declared
# `latest`; only `make upg` resolves them and writes the committed mise.lock.
# Setup installs frozen from that lock and never re-resolves.

# === SECTION: settings (managed) ===
# Source: config:codegen.toolchain.mise_lockfile / mise_locked / mise_lockfile_platforms
[settings]
lockfile = true
locked = true
lockfile_platforms = ["linux-x64", "linux-arm64", "macos-x64", "macos-arm64", "windows-x64"]
[tool_config]
locked = true
# End SECTION: settings

# === SECTION: tools (managed) ===
# Source: config:python_version, config:codegen.scaffold.project.dev native toolchain floors
#         (kubectl_version, helm_version, kind_version, taplo_version, ast_grep_version,
#          gitleaks_version, scc_selector/scc_version, kubeconform_version, qlty_selector/qlty_version,
#          node_version, jscpd_selector/jscpd_version/jscpd_asset_patterns,
#          prettier_selector/prettier_version, waza_selector/waza_version/waza_version_prefix,
#          go_version, make_version)
[tools]
python = "3.13"
uv = "latest"
# Native toolchain (single source: Infra.codegen.toolchain in codegen.yaml).
kubectl = "latest"
helm = "latest"
kind = "latest"
direnv = "latest"
taplo = "latest"
ast-grep = "latest"
gitleaks = "latest"
"aqua:boyter/scc" = "latest"
kubeconform = "latest"
node = "latest"
"npm:prettier" = "3.5.3"
go = "latest"
make = "latest"
[tools."github:qltysh/qlty"]
version = "0.642.0"
github_attestations = false
[tools."github:kucherenko/jscpd"]
version = "latest"
[tools."github:kucherenko/jscpd".platforms]
linux-x64 = { asset_pattern = "jscpd-linux-x64-gnu.tar.gz" }
linux-arm64 = { asset_pattern = "jscpd-linux-arm64-gnu.tar.gz" }
macos-x64 = { asset_pattern = "jscpd-darwin-x64.tar.gz" }
macos-arm64 = { asset_pattern = "jscpd-darwin-arm64.tar.gz" }
windows-x64 = { asset_pattern = "jscpd-windows-x64-msvc.tar.gz" }
[tools."github:microsoft/waza"]
version = "latest"
version_prefix = "v"
# End SECTION: tools
