# Generated by `flext-infra codegen conform`.
# NOTE (multi-agent, mro-wkii.17.4 / agent: codex): Python and uv are exact
# fleet-wide pins; project tools come exclusively from locked dependency groups.

[tools]
python = "3.13"
uv = "0.11.29"
