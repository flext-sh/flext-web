# Generated by `flext-infra codegen conform`.
# NOTE: mise carries ONLY language runtimes (python, uv) as exact fleet-wide
# pins from the config SSOT (Infra.codegen.toolchain). Linters/type-checkers
# come exclusively from the locked dependency groups (pyproject dev group),
# installed into the workspace .venv by uv; gates invoke them via python -m,
# so mise never pins linter tools.

[tools]
python = "3.13.11"
uv = "0.11.29"
# Deploy toolchain (single source: Infra.codegen.toolchain in codegen.yaml).
kubectl = "1.32.0"
helm = "3.19.4"
kind = "0.31.0"
