# Generated by flext_infra codegen for flext-web — DO NOT EDIT
# === SECTION: header (managed) ===
# Source: template (base/.github/workflows/ci.yml.j2)
# Free: no
# End SECTION: header

name: CI

# === SECTION: triggers (managed) ===
# Source: config:repository_branch; promotion is integration -> main only
"on":
  pull_request:
    branches:
      - 0.12.0-dev
# End SECTION: triggers

# === SECTION: permissions (managed) ===
# Source: template (minimal read-only permissions)
permissions:
  contents: read
# End SECTION: permissions

jobs:
  # === SECTION: ci job (managed) ===
  # Source: template + config:github_actions.*
  ci:
    name: ci
    if: ${{ github.head_ref == '0.12.0-dev' }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      CI: Y
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          submodules: false
          fetch-depth: 0

      - name: Install mise toolchain
        uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14  # v4.2.3

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # v9.0.0


      - name: setup (blocking)
        run: make setup

      - name: gen (blocking)
        run: make gen APPLY=Y

      - name: fmt (blocking)
        run: make fmt APPLY=Y

      - name: fix (blocking)
        run: make fix APPLY=Y

      - name: check (blocking)
        run: make check

      - name: test (blocking)
        run: make test

    # End SECTION: ci job
