# Generated by scripts/github/sync_workflows.py - DO NOT EDIT
name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: false
          installer-parallel: true

      - name: Install system build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libldap2-dev libsasl2-dev libssl-dev

      - name: Bootstrap base.mk
        run: |
          if [ ! -f base.mk ]; then
            curl -fsSL "https://raw.githubusercontent.com/flext-sh/flext/0.10.0-dev/base.mk" -o base.mk
          fi

      - name: Setup
        run: make setup

      - name: Install CI gate toolchain
        run: |
          python -m pip install --upgrade pip
          python -m pip install mypy pyright pyrefly ruff bandit pip-audit

      - name: Check
        run: make check

      - name: Test
        run: make test

      - name: Validate
        run: make validate
